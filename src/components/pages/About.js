import React from 'react'

function About() {
    return ( <
        React.fragment >
        <
        h1 > About < /h1> <
        p > This is the todo application version 1.0 < /p> < /
        React.fragment >
    )
}

export default About;